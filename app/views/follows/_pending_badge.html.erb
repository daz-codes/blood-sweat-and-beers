<%# Locals: count (Integer) %>
<% if count > 0 %>
  <span class="inline-flex items-center justify-center w-4 h-4 bg-lime-400 text-zinc-950 text-xs font-bold rounded-full"><%= count %></span>
<% end %>
