<%# Locals: count (Integer) %>
<% if count > 0 %>
  <span class="inline-flex items-center justify-center w-4 h-4 bg-orange-500 text-white text-xs font-bold rounded-full"><%= count %></span>
<% end %>
