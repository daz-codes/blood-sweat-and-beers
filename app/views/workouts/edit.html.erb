<% content_for :title, "Edit Workout" %>

<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <%= link_to "← My Library", library_path, class: "text-gray-500 hover:text-gray-300 text-sm" %>
  </div>

  <%= turbo_frame_tag "workout_preview" do %>
    <h1 class="text-2xl font-bold tracking-tight mb-1">Edit Workout</h1>
    <p class="text-gray-400 text-sm mb-6">Update sections and exercises, then save.</p>

    <%= render "workouts/builder_form",
          workout: @workout,
          action_url: workout_path(@workout),
          submit_label: "Save Changes",
          form_method: :patch,
          cancel_url: workout_path(@workout) %>
  <% end %>
</div>
